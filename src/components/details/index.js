import { Menu } from "../menu/menu"
import { PokemonDetails } from "./details"

export const Details = ( ) => {
    
    return(
        <div >
            <Menu/>
            <PokemonDetails/>
        </div>
    )
}